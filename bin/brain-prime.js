import basicOfGames from "../index.js";
import {primeGame} from "../src/games/primeGame.js";

const brainPrimeQuestion='Answer "yes" if given number is prime. Otherwise answer "no".';

basicOfGames(brainPrimeQuestion,primeGame);
